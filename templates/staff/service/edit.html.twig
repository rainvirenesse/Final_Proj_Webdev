{% extends 'admin/partials/layout.html.twig' %}

{% block title %}Edit Service{% endblock %}

{% block body %}
<div class="container-fluid py-3 admin-workspace">
    <div class="row">
        <div class="col-12 admin-detail-panel">
            <div class="card shadow-sm">
                <div class="card-header bg-white border-bottom">
                    <h5 class="mb-0 fw-semibold">Edit Service: {{ service.name }}</h5>
                </div>
                <div class="card-body p-4">
                    {{ include('admin/service/_form.html.twig', {'button_label': 'Update', 'show_button': false}) }}
                </div>
                <div class="card-footer bg-white border-top">
                    <div class="d-flex gap-2 justify-content-end">
                        <a href="{{ path('staff_service_index') }}" class="btn btn-outline-secondary">← Back to list</a>
                        <button type="submit" form="service-form" class="btn btn-dark">
                            <i class="bi bi-check-circle me-2"></i>Update
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        border: 1px solid var(--admin-border);
        border-radius: 0.5rem;
    }
    .card-header {
        padding: 1.25rem 1.5rem;
    }
    .card-footer {
        padding: 1.25rem 1.5rem;
    }
</style>
{% endblock %}

